import React from 'react';

interface ButtonProps {

}

const Button = (props: ButtonProps) => {
  return (
    <div>
      Button
    </div>
  );
};

Button.propTypes = {

};

export default Button;
